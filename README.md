
  # feeti dev front

  This is a code bundle for feeti dev front. The original project is available at https://www.figma.com/design/3Doid1Z3Inqe3w4uiL8PJk/feeti-dev-front.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  